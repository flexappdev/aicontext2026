# Skills — AI Context 2026

Claude Code skills in `.claude/skills/` — reusable, invocable workflows.

Invoke with `/skill-name` or via the Skill tool.

---

## All Skills (32)

| Skill | Description |
|-------|-------------|
| `add` | Add any feature end-to-end with page, nav, PRD doc, QA test plan, build, and optional deploy |
| `agents-md` | Generate and maintain AGENTS.md files — the standard format for guiding AI coding agents in repositories |
| `ai` | Manage AI provider configuration — API keys, model selection, provider health checks, and cost tracking |
| `backlog` | Run the full backlog lifecycle — review PBIs, assign skills, test DONE items, implement WIP items, update docs and history |
| `cleanup` | Clean up the AgentAI codebase — fix lint issues, remove unused imports/deps, optimize bundle |
| `component` | Build reusable UI components following AgentAI design patterns — theme-aware styling, TypeScript interfaces, consistent structure |
| `db` | Database operations for AgentAI — migrations, seeding, and schema verification |
| `deploy` | Pre-deployment verification — runs build, type-check, lint, and summarizes QA status |
| `doc` | Generate and update project documentation — PRDs, test plans, API docs, and the skills reference |
| `env` | Manage environment configuration — validate .env files, check required variables, compare environments, and generate templates |
| `feature` | Build a complete feature end-to-end in AgentAI — page, API, types, and server actions |
| `github` | GitHub operations via gh CLI — starred repos, repo analysis, issues, PRs, releases, and integration discovery |
| `history` | Manage session history — log new sessions, update docs/history.md, and keep the history page in sync |
| `import` | Import data from external sources — JSON files, CSV, API responses, or other AgentAI instances |
| `knowledge` | Agent knowledge and memory management — build knowledge graphs, manage context windows, create RAG pipelines |
| `migrate` | Assist with codebase migrations — dependency upgrades, API changes, schema migrations, and pattern refactors |
| `monitor` | Monitor long-running lab processes — build pipelines, data imports, AI generation jobs, and background tasks |
| `perf` | Analyze application performance — bundle size, build times, component complexity, and database query patterns |
| `persona` | Manage persona lifecycle — create, version, clone, compare, and bulk operations |
| `push` | Push current changes to the main branch — stage, commit with description, push to origin main |
| `qa` | Full QA lifecycle — run all tests, debug failures, add bugs to backlog, fix issues, and update status |
| `release` | Manage releases — version bumping, changelog generation, release notes, and tag creation |
| `research` | Deep research for development decisions — web search, repo analysis, library comparison, and architecture review |
| `review` | Code review for AgentAI project conventions — quality, theme consistency, and pattern adherence |
| `rich-ui` | Design and build rich, interactive UI responses for agents — structured cards, forms, dashboards, and data visualizations |
| `scaffold` | Generate new pages, API routes, or server actions following AgentAI project patterns |
| `seed` | Manage seed data — generate, load, reset, and export seed datasets for skills, personas, agents, and orgs |
| `spec` | Spec-driven development — brainstorm requirements, write specs, break into chunks, and execute with subagents |
| `test` | Run QA tests against AgentAI routes and features |
| `tiktok` | TikTok integration skill |
| `ux` | Review the whole site UX flow across mobile, tablet, and desktop — responsiveness, accessibility, and visual consistency |
| `vercel` | Vercel operations — sync env vars, manage deployments, check status, promote, rollback, and configure project settings |

---

_Skills path: `.claude/skills/<name>/SKILL.md`_
